# TWR1 公开候选验证 · 2026-09-24

仅离线构建与静态导出；本次发布准备没有安装手机、没有刷眼镜、没有请求账号接口。

## 已完成

- 从公开 `native-navigation/src` 加 `weread/src` 重建 AP，SHA-256 与实刷精确样本相同：`66c88f54c12ad129d8d568261e399350201df47e0ce9904cf8440e03e1f90ab4`。
- ZIP 15个成员相同；重新压缩 ZIP 的时间戳可变，不当作手机门禁授权。发布附件使用实刷原始 ZIP，仅改文件名，SHA-256 `a7a1e98dd22bd45a87bba5f8c19862d5fa3a5f0d5877ed4330cca8face75575c`。
- 14项负载仅 AP 改变；与原厂逐项比较，其他13项字节相同，清单只有 AP 的 Size/MD5 改变。
- 链接后 ARM：94组菜单；阅读四封面/窗口/回传/父页面销毁；打开前后渲染忙、超时、取消、分配/常亮失败与重试；音乐、导航、诊断和单帧显示回归通过。平台、LVGL和系统状态为测试替身。
- ASAN/UBSAN：阅读双 bank、1000轮接收/滚动/释放、CRC/UTF-8/旧会话、视图资源解绑和分配失败、旋钮阈值通过。
- 手机原创模块：统计、TXT/EPUB与窗口、传输队列测试通过；真实传输源文件单独编译。8个合成手机包成功回放到本次链接后 AP。
- 去掉网页适配后的 ReaderUI/WeReadAPI 通过 iOS arm64 语法编译；这不是整套宿主链接、安装或镜片验收。
- 发布校验器拒绝改动 AP、改动非 AP、改动清单及额外路径四类负例。
- 源码发布目录经脱敏秘密扫描；固件检查未发现维护者 API Key、Cookie 或本机路径。原厂 AP 的5处 PEM 私钥格式标签为短小 NUL 结尾识别字符串，与原厂相同，不是维护者私钥。

## 仍未完成

- 长按旋钮返回异常未修复；旋钮真实手感、完整正文跨窗口/跨章、所有封面、长期后台与全部原厂功能未全量验收。
- 公开手机模块的默认构建、菜单接线和 TWR1 OTA 打包门禁未集成；不能把本次源码模块当成已完成手机产品。
- 网页正文适配不在公开目录；私用版验证不可直接当作公开版验证。
- 不保证原厂回滚、断电恢复或救砖。

实机证据仅沿用用户此前确认：书架同步可用，以及本精确固件升级完成后回到首页。不声称本次重新做过真机回归。
