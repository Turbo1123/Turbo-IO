# 唤醒等待与时间反馈

用户已批准将持续 ASR 首次无语音等待缩短到 8 秒，并检查欢迎页及时间显示。

设计：在现有 StandbyVoiceSession 中单独记录首次等待截止时间，收到当前会话的有效识别后取消；退出、断连和重连不能沿用旧计时。保持现有回答后 10 秒续说、识别/模型保护及 Hermes 独立任务生命周期。手机显示当前本地时间和状态对应的倒计时。

协议边界：已核对的 type5 是真实 ASR 文字，type4 是真实语音边界；未找到独立欢迎语或固定时钟接口。不伪造语音事件，也不把时钟当作用户话语发送。眼镜本轮改善是更快退出静默欢迎页；固定时钟和替换默认欢迎语尚未实现。

实施与验收：
- [x] 在 `core-probe/Tests/StandbyWakeTests.swift` 重现静默 8 秒未退出，记录 red 输出。
- [x] 修改 `core-probe/Sources/StandbyVoiceSession.swift`：首次 8 秒等待、有效识别取消、按会话清理、只读剩余时间。
- [x] 经 `core-probe/Sources/App.swift` 把剩余时间传至现有 `CompanionVoiceRuntime.swift`，在 `VoiceConversationView.swift` 展示手机时钟、正在听及倒计时。
- [x] 执行 `sh scripts/run_voice_wake_tests.sh`；补充计时、迟到事件、断连、退出失败及真实转写不混入状态的测试。
- [x] 构建现有真机工程、原位安装；不修改签名、生成工程、现有凭据、框架和配对，不重启 Hermes/Codex。
- [x] 保存本次增量 diff 和测试输出，等待用户眼镜显示反馈；不把协议提交当作镜片渲染确认。

替代方案：直接缩短全部收音上限会截断用户讲话，因此不采用；用假 ASR 文本覆盖欢迎页会混淆对话含义，因此不采用。首次计时以有效云识别为取消依据；音频包本身不能证明有人说话，云识别迟到时仍可能触发保护退出。

交付：10 项状态机测试通过，真机构建通过，已原位安装并打开会话页。已请用户验证约 8 秒的实际镜片退出及正常说话；反馈尚未收到。记录见 `artifacts/voice-wake/`。
